=====================================================================
 RUN COMMANDS - Emissivity Estimation Tool (ZED 2i)
=====================================================================

--- WINDOWS ---
cd C:\Users\loren\desktop\measurment_v2\ClaudeCOde\Thesis\EmissivityCalculation
set HF_HUB_OFFLINE=1&& set TRANSFORMERS_OFFLINE=1&& C:\venvs\emissivity\Scripts\python.exe main.py --zed-uvc --show --live


--- LINUX / UBUNTU (server/morpheus) - WORKING HEADLESS, SHARED CAMERA ---
# Headless (no GUI), reads the right eye from a running camera_server.py.
# Start camera_server.py FIRST (see CameraServer/run_commands.txt), then:
cd ~/Desktop/LorenzoWidmann/Code/Thesis/EmissivityCalculation
source .venv/bin/activate
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python main.py --shared

# ONE-TIME FIX (only if it crashes with
#   AttributeError: module 'PIL.Image' has no attribute 'Resampling'):
# system Pillow is too old for transformers/CLIP. Inside the venv:
pip install --upgrade 'Pillow>=9.1'

--- LINUX / UBUNTU - GUI / direct camera (no server) ---
cd ~/Desktop/LorenzoWidmann/Code/Thesis/EmissivityCalculation
source .venv/bin/activate
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python3 main.py --zed-uvc --camera-index 0 --show --live

# if index 0 does not work, try index 1:
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python3 main.py --zed-uvc --camera-index 1 --show --live

# NOTE: HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE force use of the cached CLIP
# model and skip the HuggingFace hub freshness check (avoids slow 504 retry
# loop when the hub is unreachable). Drop the prefix to allow checking for
# model updates again.

# --- FIRST RUN with the new default model (openai/clip-vit-large-patch14) ---
# Needs network access to download it once (~1.7 GB). Do NOT set
# HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE for this one run:
python3 main.py --zed-uvc --camera-index 0 --show --live
# After that it's cached and the normal offline command above works.

# The original smaller/faster model is still cached and usable via --clip-model:
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python3 main.py --zed-uvc --camera-index 0 --show --live --clip-model openai/clip-vit-base-patch32
=====================================================================
