# Regenerated by every run of solar_shadow_voxel.py (VOSTOK's inputs, VOSTOK's
# own shadow_clouds/ output, and the parsed shadow_mask.json). Never commit:
# they are large and are only valid for the --north-bearing-deg they were run
# with.
solar_shadow_voxel_out/

__pycache__/
*.py[cod]
