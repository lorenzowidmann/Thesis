# Everything the pipeline generates (planes.json, voxels.npz, transform.json,
# planes_aligned.json, screenshots/GIFs). Build products of a specific bag +
# --voxel-size, regenerated by every run -- never commit.
OcTreeVoxel_out/

__pycache__/
*.pyc
interactive_viewer.log
orbit.gif
